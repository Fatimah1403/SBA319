<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mobile device</title>
</head>
<body>
    <h1>Welcome to my Mobile Store</h1>

    <% if (brands.length > 0) { %>
        <h2>Brands</h2>
        <ul>
            <% brands.forEach(brand => { %>
                <li><%= brand.name %> - <%= brand.country %></li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>No brands available.</p>
    <% } %>
</body>
</html>